<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DeputyApp.Pages.DocumentsPage"
             Title="Документы">

    <CollectionView x:Name="DocsList" Margin="10">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame BorderColor="#27AE60" CornerRadius="10" Padding="10" Margin="5">
                    <VerticalStackLayout>
                        <Label Text="{Binding Title}" FontSize="18" FontAttributes="Bold"/>
                        <Label Text="{Binding UploadedAt, StringFormat='Загружено: {0:dd.MM.yyyy}'}" TextColor="Gray"/>
                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>